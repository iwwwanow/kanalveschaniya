{
  "name": "kanalveshchaniya",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "6.0.0",
    "prettier": "3.6.2",
    "typescript": "5.9.3"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "db:push": "TODO pnpm run --env-file=.env.dev --filter '@infrastructure' db:push",
    "db:generate": "TODO pnpm run --env-file=.env.dev --filter '@infrastructure' db:generate",
    "db:migrate": "TODO pnpm --env-file=.env.dev --filter '@infrastructure' db:migrate"
  }
}
